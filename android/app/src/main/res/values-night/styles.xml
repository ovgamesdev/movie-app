<resources>

		<!-- Base application theme. -->
		<style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
				<!-- Customize your theme here. -->
        <item name="android:editTextBackground">@drawable/rn_edit_text_material</item>
    </style>

		<!-- yarn react-native generate-bootsplash src/assets/images/logo.png \ -!remove!-platforms=android -->
    <!-- NOTE brand size: hdpi 300x120 | mdpi 200x80 | xhdpi 400x160 | xxhdpi 600x240 | xxxhdpi 800x320 -->
    <!-- BootTheme should inherit from Theme.BootSplash or Theme.BootSplash.EdgeToEdge -->
    <style name="BootTheme" parent="Theme.BootSplash">
        <item name="android:navigationBarColor">@color/bootsplash_background_dark</item>

        <item name="bootSplashBackground">@color/bootsplash_background_dark</item>
        <item name="bootSplashLogo">@drawable/bootsplash_logo</item>
        <!-- <item name="bootSplashBrand">@drawable/bootsplash_brand</item> Only if you have a brand image -->
        <item name="postBootSplashTheme">@style/AppTheme</item>
    </style>
</resources>
